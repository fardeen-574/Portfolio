{
  "name": "mern-portfolio",
  "version": "1.0.0",
  "description": "Full Stack Developer Portfolio – MERN Stack",
  "scripts": {
    "install-all": "npm install && cd server && npm install && cd ../client && npm install",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": " npm run dev",
    "client": "npm run dev",
    "build": "npm run build"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
